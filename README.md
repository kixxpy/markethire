# Markethire

Платформа для связи селлеров маркетплейсов (Wildberries, OZON) с исполнителями услуг.

## 🚀 Быстрый старт

### Требования

- Node.js 18+ 
- PostgreSQL
- npm или yarn

### Установка

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```

4. Настройте переменные окружения в `.env`:
   - `DATABASE_URL` - URL вашей PostgreSQL базы данных
   - `JWT_SECRET` - секретный ключ для JWT токенов

5. Примените миграции базы данных:
   ```bash
   npm run db:migrate
   ```

6. Сгенерируйте Prisma Client:
   ```bash
   npm run db:generate
   ```

7. Запустите сидинг (заполнение категорий и тегов):
   ```bash
   npm run db:seed
   ```

8. Запустите сервер разработки:
   ```bash
   npm run dev
   ```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## 📁 Структура проекта

```
/prisma          - Prisma схема и миграции
/src
  /api           - API эндпоинты
  /lib           - Утилиты и конфигурация
  /types         - TypeScript типы
  /middleware    - Middleware функции
  /services      - Бизнес-логика
  /seed          - Сидинг данных
/public          - Статические файлы
/pages           - Next.js страницы
/tests           - Тесты
```

## 🛠️ Доступные команды

- `npm run dev` - Запуск сервера разработки
- `npm run build` - Сборка для продакшена
- `npm run start` - Запуск продакшен сервера
- `npm run lint` - Проверка кода линтером
- `npm run db:generate` - Генерация Prisma Client
- `npm run db:migrate` - Применение миграций
- `npm run db:studio` - Открытие Prisma Studio
- `npm run db:seed` - Заполнение базы данных начальными данными

## 📚 Технологии

- **Next.js** - React фреймворк
- **TypeScript** - Типизированный JavaScript
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** - База данных
- **JWT** - Аутентификация
- **Zod** - Валидация данных

## 🔐 API Эндпоинты

### Аутентификация

#### Регистрация
```http
POST /api/auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123",
  "name": "Имя пользователя",
  "role": "SELLER" // опционально: SELLER, PERFORMER, BOTH (по умолчанию)
}
```

#### Вход
```http
POST /api/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

### Профиль пользователя

#### Получение профиля
```http
GET /api/users/me
Authorization: Bearer <token>
```

#### Обновление профиля
```http
PATCH /api/users/me
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "Новое имя",
  "description": "Описание профиля",
  "priceFrom": 1000,
  "telegram": "@username",
  "whatsapp": "+79991234567",
  "emailContact": "contact@example.com",
  "role": "BOTH"
}
```

### Роли пользователей

- **SELLER** - Селлер (может создавать задачи)
- **PERFORMER** - Исполнитель (может откликаться на задачи)
- **BOTH** - Оба (может и создавать задачи, и откликаться)

## 📝 Лицензия

ISC
